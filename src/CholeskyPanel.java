import javax.swing.*;
import java.awt.*;

public class CholeskyPanel extends JPanel {
    public CholeskyPanel() {
        setLayout(new BorderLayout());
        JLabel label = new JLabel("Cholesky’s Method GUI - Work in Progress");
        add(label, BorderLayout.NORTH);
    }
}