import javax.swing.*;
import java.awt.*;

public class GaussJacobiPanel extends JPanel {
    public GaussJacobiPanel() {
        setLayout(new BorderLayout());
        JLabel label = new JLabel("Gauss-Jacobi Method GUI - Work in Progress");
        add(label, BorderLayout.NORTH);
    }
}