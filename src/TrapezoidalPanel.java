import javax.swing.*;
import java.awt.*;

public class TrapezoidalPanel extends JPanel {
    public TrapezoidalPanel() {
        setLayout(new BorderLayout());
        JLabel label = new JLabel("Trapezoidal Rule GUI - Work in Progress");
        add(label, BorderLayout.NORTH);
    }
}